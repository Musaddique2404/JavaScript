"use strict"; //treat all JS code as a newer version

// alert("Hello") we are using node.js not browser 

// Code readability should be high

//use mdn for docs

//orignal docs with proper ecma script are available on tc39.es

let name = "musaddique" //String Datatype
let age = 21 //number
let isLoggedIn = true //Boolean

// number => 2 to power 53
// bigint
// string => ""
// Boolean => true or false
// null => type or standalone value (representation of empty value / it is special type we can define variable)
// undefined => When variable is declared and value is not declared than it is called undefined 
// symbol => unique

/* object => an object is a collection of key-value pairs enclosed in {}. Example:
let person = { name: "John", age: 30 };
console.log(person.name); // Output: John
*/

console.log(typeof undefined); //undefined
console.log(typeof null); //object

